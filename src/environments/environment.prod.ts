import { EnvironmentInterface } from './environment.interface';

export const environment: EnvironmentInterface = {
    baseUrl: 'https://paynut.appspot.com',
    production: true,
    tokenKey: 'AUTH_TOKEN'
};
